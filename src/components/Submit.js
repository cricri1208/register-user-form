import React from "react";
import {Container} from "@mui/material";

 export const Submit =()=>{

    return (
        <Container maxWidth="sm" style={{marginTop:'4rem'}}>
            <h3>Thank you for submitting, we will contact you!</h3>
        </Container>
    )
}